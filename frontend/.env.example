# Production: set in Vercel → Project Settings → Environment Variables
# Example after deploying backend to Render:
# VITE_API_URL=https://legionhealth-apo-api.onrender.com/api

# Leave unset for local dev (Vite proxies /api → localhost:8000)
# VITE_API_URL=
